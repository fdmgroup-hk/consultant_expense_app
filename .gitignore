# secrets
.env
*.key

# data & uploads (consultant material must never be committed)
data/
!data/.gitkeep

# python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# deployment secrets for copy-paste into Render - never commit
RENDER_ENV.local.txt

# Render API key - never commit
render_key.txt

# deployment env values (Supabase/Render) - never commit
.env.render

# Cloudflare token - never commit
cf_token.txt
cf_account.txt
